name: "Build RemoteCam Final"

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          npm install
          npx cap add ios
          npx cap sync ios

      - name: Inject Camera & Garmin SDK
        run: |
          # 1. On fusionne ton code CameraManager dans l'AppDelegate
          cat <<'EOF' > ios/App/App/AppDelegate.swift
          import UIKit
          import Capacitor
          import AVFoundation
          import ConnectIQ

          @UIApplicationMain
          class AppDelegate: UIResponder, UIApplicationDelegate {
              var window: UIWindow?
              lazy var cameraManager = CameraManager()
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  cameraManager.setup()
                  let previewLayer = AVCaptureVideoPreviewLayer(session: cameraManager.session)
                  previewLayer.videoGravity = .resizeAspectFill
                  DispatchQueue.main.async {
                      if let rootVC = self.window?.rootViewController {
                          previewLayer.frame = rootVC.view.bounds
                          rootVC.view.layer.insertSublayer(previewLayer, at: 0)
                          rootVC.view.backgroundColor = .clear
                          for subview in rootVC.view.subviews {
                              subview.backgroundColor = .clear
                          }
                      }
                  }
                  return true
              }
          }
          extension UIImage {
              func resized(to size: CGSize) -> UIImage? {
                  UIGraphicsBeginImageContextWithOptions(size, false, 0.0)
                  draw(in: CGRect(origin: .zero, size: size))
                  let newImage = UIGraphicsGetImageFromCurrentImageContext()
                  UIGraphicsEndImageContext()
                  return newImage
              }
          }
          EOF
          grep -v "import " CameraManager.swift >> ios/App/App/AppDelegate.swift

      - name: Install Pods (Garmin)
        run: |
          cd ios/App
          pod install --repo-update

      - name: Build App Binary
        run: |
          cd ios/App
          # On compile le binaire sans tenter d'exporter (pour éviter l'erreur de signature)
          xcodebuild build \
            -workspace App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      - name: Create Unsigned IPA (Sideloadly compatible)
        run: |
          mkdir -p Payload
          # On cherche le fichier .app généré
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "App.app" -type d | head -n 1)
          cp -r "$APP_PATH" Payload/
          zip -r RemoteCam_Ready.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: RemoteCam-iOS
          path: RemoteCam_Ready.ipa
