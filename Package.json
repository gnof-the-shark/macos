{
  "name": "remotecam-garmin",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/",
    "res": "capacitor-assets generate --ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/assets": "^3.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
